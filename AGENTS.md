# AGENTS.md - 配置与规则维护规范

本文件的作用域是 `codex-config` 仓库的维护。`AGENTS.global.md`、`config.toml`、`models/*`、`skills/*` 和 `install.sh` 是本仓库产出的可分发制品，根目录 `AGENTS.md` 是项目级开发规则。

## 制品契约

- `AGENTS.global.md` 是安装后始终生效的通用默认规则，作为用户级 `AGENTS.md` 独立生效；能力选择前必须生效的契约写在这里。
- 每个 `skills/<name>/` 是可单独安装的能力包，由 `SKILL.md`、`agents/openai.yaml` 和包内资源构成完整契约。
- `config.toml`、`models/*` 和 `install.sh` 共同定义运行配置、模型目录及安装行为；README 准确描述这些制品的当前契约。
- `install.sh` 分发仓库制品和 `skills/*`，并处理本仓库拥有的已知旧安装路径；根目录 `AGENTS.md` 保持项目级作用域。

## 全局规则编写

- 在能力选择之前就必须生效，或不论使用何种能力都成立的通用契约，写入 `AGENTS.global.md`；选中某项能力后才需要的领域知识、执行流程、分支条件和交付方法，写入对应 skill。
- 按 Agent 最早必须知道一条规则的时点组织内容，使授权、范围、工作区保护、执行流程、验证和交付边界在需要决策前已经明确。
- 合并相同主题和相同触发条件的规则，保持单一权威表述；避免用路由说明替代实际行为契约。

## Skill 编写

- `name` 使用简短、稳定且动作导向的名称。
- `description` 仅描述 skill 能完成的工作，包括处理对象、主要动作和有意义的能力边界；Agent 应能仅凭 `name` 和 `description` 识别该能力。
- `SKILL.md` 正文假定该能力已被选中，完整描述执行所需的流程、判定条件、安全边界、验证和交付标准。
- Skill 的执行依赖由用户任务、作用对象、可观察状态和包内资源完整表达，使其在其他合理的全局规则下仍能独立执行。
- Skill 只包含完成该能力所需的程序性知识、领域知识和可复用资源；`agents/openai.yaml` 的展示名称、简介和默认提示与 `SKILL.md` 保持一致。
- 用户明确要求仅显式调用时，在 `agents/openai.yaml` 设置 `policy.allow_implicit_invocation: false`；不得因事故、纠正或领域敏感程度自行启用或禁用。

## 表达与维护

- 规则优先正向定义应执行的行为、触发条件和预期结果。
- 禁止和例外用于表达必要边界，或纠正已有观察证据支持的失败模式与行为倾向。问题源于原规则表述不清或存在歧义时，先澄清原规则；只有澄清后仍需约束已观察行为时才增加禁止或例外。
- 每条规则优先表达要完成的行为、边界和结果，并使用可观察、可验收的对象、条件和动作表述。只有实现机制本身影响核心意图时才约束工具选择，不把模型已经掌握的命令和参数写成操作手册。
- 新证据优先用于修订、合并或替换已有规则。新条款只用于已有结构无法表达的独立契约。
- 项目规则只保留稳定、可复用的制品维护契约。一次性背景、实施过程和过往问题记录到 Git 历史或任务笔记。
- 修改全局行为时同步递增 `AGENTS.global.md` 的版本号。

## 变更审查

- 新增或移动规则时，用最早生效时点验证其章节位置。
- 拆分或重写时对照原契约的作用域、触发条件、默认行为、例外和停止条件，确认每项语义均被明确保留、修订或删除。
- 修改规则或安装契约的提交信息必须保留语义决策：正文记录调整原因、要纠正或保留的核心意图、关键边界与取舍，使后续维护者无需仅凭当前措辞推测设计意图。
- 验证 `AGENTS.global.md` 的通用契约完整性、内部一致性和安装后的实际生效副本，并验证每个 skill 的可发现性与在合理全局规则下的可执行性。
- 修改可分发制品后运行 `install.sh` 并核对安装副本；修改项目规则时核对安装范围。
- 完成前运行 `git diff --check`；安装器变化时同时运行 `bash -n install.sh`。
